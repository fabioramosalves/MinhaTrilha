package lambdas;

import java.util.function.Function;

public class Funcao {

	public static void main(String[] args) {
		Function<Integer, String> isPar = (p)-> (p % 2 == 0 ? "Par" :"Impar");
		
		System.out.println(isPar.apply(3)); 
		System.out.println(isPar.apply(4)); 
	}
}
